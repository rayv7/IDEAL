body: Stack(
        children: [
          // Layer 1: Main Scrolling Surface
          ListView(
            padding: EdgeInsets.zero, // Removes any automatic system padding
            children: [
              // 1. Spacing to slide elements underneath the 80px floating header
              const SizedBox(height: 80),

              // 2. Placeholder for our upcoming Auto-scrolling Carousel
              Container(
                height: 300,
                margin: const EdgeInsets.all(16),
                decoration: BoxDecoration(
                  color: const Color(0xFFE8F5E9), // Light green background card
                  borderRadius: BorderRadius.circular(16),
                ),
                alignment: Alignment.center,
                child: const Text(
                  'Promo Carousel Placeholder',
                  style: TextStyle(fontSize: 18, color: Color(0xFF2E7D32)),
                ),
              ),

              // 3. Placeholder for Category Track
              const Padding(
                padding: EdgeInsets.symmetric(horizontal: 16, vertical: 8),
                child: Text(
                  'Category Track',
                  style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
                ),
              ),
              
              // We will add the actual sliding widgets here next!
            ],
          ),

          // Layer 2: Floating Translucent Header (Keep your existing code here below)